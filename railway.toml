[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build && npm run setup-folders && node setup-imagekit.js"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"

[postgres]
name = "conexion360-db"
version = "15"

# Variables de entorno para Railway
[variables]
NODE_ENV = "production"
HOSTNAME = "0.0.0.0"

# Variables ImageKit (configurar en Railway Dashboard)
# IMAGEKIT_PUBLIC_KEY = "public_nJIM9VeYDWasBIUi3ixlGpRzZz4="
# IMAGEKIT_PRIVATE_KEY = "private_OiWhfp78ou3Prah0GLZ67xoLE98="
# IMAGEKIT_URL_ENDPOINT = "https://ik.imagekit.io/qpdyvnppk"